package com.milasoraki.tokiefy.ui.theme

import androidx.compose.material3.Typography

/**
 * Typography tokens.
 *
 * Defaults are used verbatim because the Compose Material 3 defaults are
 * already close to TikTok's sans-serif look; sizes are overridden
 * per-component in the screens themselves to match the screenshot
 * exactly (large bold nicknames, 11–12sp badges).
 */
public val TokiefyTypography: Typography = Typography()
